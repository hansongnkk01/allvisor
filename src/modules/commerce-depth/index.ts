/** Commerce depth — variants / serials / price tiers. */
export const fashionCapabilities = ["variants", "pos"] as const;
export const electronicsCapabilities = ["serial_numbers", "pos"] as const;
export const wholesaleCapabilities = ["price_tiers", "pos"] as const;
