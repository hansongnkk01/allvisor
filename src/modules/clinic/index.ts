/** Clinic niche — appointments & patient-oriented labels live in app routes + i18n */
export { AppointmentBoard, AppointmentBoardLazy } from "@/modules/booking";
export const clinicFeatures = ["appointments", "patients", "invoices"] as const;
