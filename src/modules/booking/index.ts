/**
 * Booking module — appointments, scheduling, and clinic-hours UI.
 *
 * Booking routes (/appointments) should load via this module so appointment
 * code stays isolated from commerce and other niche bundles.
 */
export { AppointmentBoard } from "@/components/AppointmentBoard";
export { AppointmentBoardLazy } from "@/components/AppointmentBoardLazy";
